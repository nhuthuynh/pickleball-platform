# node_modules is reinstalled inside the image via `npm ci`; copying the
# host's (possibly platform-specific) tree in would be slow and wrong.
node_modules
dist
.vite

# Not needed to build the bundle.
**/__tests__
*.md
Dockerfile
.dockerignore
